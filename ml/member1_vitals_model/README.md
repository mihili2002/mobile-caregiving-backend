# Member 1 — Vitals Model

Training code for vitals prediction. This folder is dedicated to training only.

Do not import from this package in the API runtime. Export trained artifacts
to `ml/trained_models` and let the backend load them at inference time.
